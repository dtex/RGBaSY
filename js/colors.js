var since = 0, myWidth = 50;var query = 'http://rgby.spas.io?callback=loaded';var w = new Array();$(function() {	loadMore();	$(window).resize( function() {		$('#container').height($(window).height());		$('#container div').height($(window).height());	});});loadMore = function(lsince) {  $.getScript(query);}loaded = function(data) {    console.dir(data.searchTweets.result);    w = data.searchTweets.result.statuses;    drawMore(w);};clean = function(tIn) {  out = tIn.toLowerCase().replace(/\W|_/gi, ' ');  return out;}var c = 0;drawMore = function(w) {    c++;    var thisClass = clean(w[w.length-1].text);    console.log(w[w.length-1]);    $('#container').prepend('<div id="' + w[w.length-1].id + '" class="' + thisClass + '" style="width:0px;float:left;display:none;height:' + $(window).height() + 'px;">&nbsp;</div>');    since =  w[w.length-1].id;    if ($('#' + w[w.length-1].id).css('backgroundColor') == 'transparent' || $('#' + w[w.length-1].id).css('backgroundColor') == 'rgba(0, 0, 0, 0)') {        $('#' + w[w.length-1].id).style.visibility = 'hidden';        w.pop();        w.length == 0 ? setTimeout(loadMore(since), 5000) : drawMore(w);    } else {        $('#container').css('width', ($('#container').width() + myWidth) + 'px');        $('#container').height($(window).height());        $('#container > div:first').animate({ width: myWidth + 'px' }, 1000, function() {            w.pop();            if (c < 50) {              w.length == 0 ? setTimeout(loadMore(since), 5000) : drawMore(w);            }        });    }}/*trimWhiteSpace = function(myString, j) {	while (myString.substring(0,1) == ' ') myString = myString.substring(1);	while (myString.substring(myString.length) == ' ') myString = myString.substring(0, myString.length-1);	return myString;}*//*var t=setTimeout("window.location.reload( true );",10000);*/